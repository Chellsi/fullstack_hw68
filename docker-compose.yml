version: '3.9'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      MONGODB_URI: mongodb+srv://alexsarnavskiy_db_user:mFMFPU2DXfgGig0S@cluster0.glulr8q.mongodb.net/?appName=Cluster0
      PORT: 3000
    volumes:
      - ./:/app
      - app-node-modules:/app/node_modules
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  app-node-modules:
  mongo-data:
